export const ThinParagraph = ({ children }) => {
    return(
        <p style={{ paddingLeft: 15, paddingRight: 15, marginTop: 0, marginBottom: 0 }}>                
            {children}
        </p>
    );
}